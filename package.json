{
  "name": "convert-vscode-extension",
  "displayName": "Convert Extension",
  "description": "Upload JS/CSS files to Convert.com variations",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": ["Other"],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "convert-panel",
          "title": "Convert",
          "icon": "src/resources/icon.svg"
        }
      ]
    },
    "views": {
      "convert-panel": [
        {
          "id": "convertDropZone",
          "name": "Drop Files Here",
          "type": "tree"
        },
        {
          "id": "convertSidebar",
          "name": "Convert",
          "type": "webview"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0"
  }
}